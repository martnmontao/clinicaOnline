import { BotonAnimadoDirective } from './boton-animado.directive';

describe('BotonAnimadoDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonAnimadoDirective();
    expect(directive).toBeTruthy();
  });
});
