import { PermisosUsuarioDirective } from './permisos-usuario.directive';

describe('PermisosUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new PermisosUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
