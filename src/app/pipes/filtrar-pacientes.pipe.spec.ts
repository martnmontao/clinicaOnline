import { FiltrarPacientesPipe } from './filtrar-pacientes.pipe';

describe('FiltrarPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
