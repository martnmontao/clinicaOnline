import { NombreFormateadoPipe } from './nombre-formateado.pipe';

describe('NombreFormateadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreFormateadoPipe();
    expect(pipe).toBeTruthy();
  });
});
