import { OrdenarPorFechaPipe } from './ordenar-por-fecha.pipe';

describe('OrdenarPorFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
